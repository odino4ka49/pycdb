/** * Created by 1ka on 4/5/14. */ZOOMPYCDB.namespace("ZOOMPYCDB.TagsController");ZOOMPYCDB.TagsController = function(model,view){    var storage = model.storage,        temp_data = model.temp,        view = view;    $(document).on("tag_added",function(event,id){		temp_data.addChosenTag(storage.getNode(function(node){return (node.id==id)&&(node.cid==149);}));    });    $(document).on("chosen_tag_removed",function(event,id){		temp_data.removeChosenTag(id);    });    $(document).on("chosen_tags_list_changed",function(event){		storage.hidTag(temp_data.getChosenTagsId());    });    return{    };};